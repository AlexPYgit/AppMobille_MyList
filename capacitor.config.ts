import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Liste de course',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
