import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'my_list_cours',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
